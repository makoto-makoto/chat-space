.chat-side
  .side-header
    .side-header__box
      %p.side-header__box--user-name 
        = current_user.name
      %ul.side-header__box--user-name
        %li.side-header__box__user-name__user
          = link_to new_group_path do
            = icon('fas' , 'edit')
        %li.side-header__box__user-name__group
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .groups
    .group
      %p.group__name
        test
      %p.group__latest-mwessage
        test 
        